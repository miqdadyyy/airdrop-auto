version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - airdrop-database:/app/MasterCryptoFarmBot/database.db
      - airdrop-modules:/app/MasterCryptoFarmBot/modules/
      - airdrop-telegram-accounts:/app/MasterCryptoFarmBot/telegram_accounts/

volumes:
  airdrop-database:

  airdrop-modules:

  airdrop-telegram-accounts:

